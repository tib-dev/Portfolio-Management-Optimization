paths:
  data:
    data_dir: "data"
    raw_dir: "data/raw"
    interim_dir: "data/interim"
    processed_dir: "data/processed"
  reports:
    reports_dir: "reports"
  logs:
    logs_dir: "logs"
  models:
    models_dir: "models"  
  